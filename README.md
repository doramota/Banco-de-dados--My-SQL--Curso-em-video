
---

# 🐬 Projeto MySQL — Banco de Dados Criado do Zero

Este repositório contém os scripts SQL que desenvolvi durante meus estudos de **MySQL** no *Curso em Vídeo*.
Todo o banco de dados foi **criado do zero**, incluindo estrutura, tabelas, relacionamentos e consultas.

## 🔧 O que foi construído

* Criação do **schema** no MySQL Workbench
* Construção manual das tabelas:

  * `cursos`
  * `gafanhotos`
  * `gafanhoto_assiste_curso` (tabela de relação N:N)
* Definição de:

  * chaves primárias
  * chaves estrangeiras
  * tipos de dados
  * constraints
* Inserção de dados com `INSERT`
* Execução de consultas que envolvem:

  * `SELECT`, `WHERE`, `ORDER BY`, `LIKE`, `BETWEEN`, `IN`
  * Funções de agregação (`COUNT`, `MAX`, `MIN`, `SUM`, `AVG`)
  * `GROUP BY` + `HAVING`
  * `ALTER TABLE` e `UPDATE`
  * `INNER JOIN`, `LEFT JOIN`, `RIGHT JOIN`
* Implementação completa do relacionamento muitos-para-muitos

## 🎯 Objetivo

Registrar meu progresso no aprendizado de SQL e demonstrar minha capacidade de **construir e manipular bancos de dados do zero**, aplicando boas práticas e comandos essenciais.

---


